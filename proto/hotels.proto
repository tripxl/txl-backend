syntax = "proto3";

package hotels;

service HotelsService {
  rpc GetAllHotels (Empty) returns (HotelList);
}

message Empty {}

message Hotel {
  string id = 1;
  string name = 2;
  string city = 3;
  string country = 4;
}

message HotelList {
  repeated Hotel hotels = 1;
}
